import 'db_client.dart';

// TODO: finish impl once interface done
class DatabaseServiceImpl
// implements DatabaseService
{
  DatabaseClient _client;

  DatabaseServiceImpl({required DatabaseClient client}) : _client = client;
}
